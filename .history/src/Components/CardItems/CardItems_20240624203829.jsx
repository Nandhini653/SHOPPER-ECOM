import React, { useContext } from 'react'
import './CardItems.css'

const CardItems = () => {
   const {all_product, cardItems, removeFromCart} = useContext()

  return (
    <div className='carditems'>
      
    </div>
  )
}

export default CardItems
