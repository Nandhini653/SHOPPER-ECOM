import React, { useContext } from 'react'

const Product = () => {
  const {all_product} = useContext(ShopContext);
  const {product}
  return (
    <div>
      
    </div>
  )
}

export default Product
